# SPEC.md — beagle-atlas(ビーグル・アトラス)

<!-- scaffold template v1.26.0 から展開(2026-09-04)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

ダーウィン『ビーグル号航海記』(*Journal of Researches*, 2nd ed., 1845)は、
**「いつ・どこで・何を見たか」が本文中にほぼ揃っている**稀な古典である。
本サイトは、これを読むためのビューアではなく、**航海日誌を構造化データとして再構成し、
地理と時間の二軸から本文へ再入場させる装置**である。

主画面は世界地図上の航路、副画面が年表とテキストペイン。
1831 年 12 月の出港から 1836 年 10 月の帰港まで、約 5 年・地球一周分の記述が一枚に載る。

**cron ゼロ・関数ゼロの完全静的配信**。データは 1836 年で確定しており更新されない。

## 2. 名前と系列

フリートの `-lens` は定期収集系(cron あり)の系列である。本作は cron を持たない
一次資料の構造化なので `-atlas` 系に属する(kiko-atlas 紀行アトラスの英語古典版にあたる)。

## 3. 底本

| 項目 | 値 |
|---|---|
| 著作 | Darwin, *Journal of Researches into the Natural History and Geology of the Countries Visited during the Voyage of H.M.S. Beagle round the World*, 2nd ed. |
| 刊行 | London: John Murray, 1845(Freeman 番号 **F14**) |
| 権利 | **パブリックドメイン**(1845 年刊・著者 1882 年没) |

**版は書誌メタデータではなく本文で確定した**(実測 2026-09-04)。根拠は序文末尾の日付と
「largely condensed and corrected」の記述。
**Project Gutenberg の書誌ページは "published in 1839" と表示するので、
メタデータを信じると版を取り違える**(AGENTS.md「外部データの採録」)。

ただし**署名の翻刻は二経路で異なる**: S-B は完全な日付(`DOWN, BROMLEY, KENT, June 9, 1845`)を
持つが、**S-A は日の数字と地名行を欠く**(`June , 1845.` = 台帳 E-02)。
したがって**両経路に要求できるのは年まで**であり、完全な日付は S-B から採る。

| ID | 役割 | 経路 | 取得日 |
|---|---|---|---|
| S-A | **正本** | Darwin Online `converted/published/1845_Beagle_F14/1845_Beagle_F14.html` | 2026-09-03 |
| S-B | **照合** | Project Gutenberg eBook #944 プレーンテキスト | 2026-09-03 |
| S-C | 船の航路 | FitzRoy, *Narrative* vol. II(F10.2)+ Appendix(F10.2a) | **未取得** |
| S-D | 日次行程 | Darwin Online, day-by-day itinerary(Rookmaaker 編) | **未取得** |

URL・バイト数・sha256・加工手順は `raw/MANIFEST.json` に固定してある。

**S-B のヘッダ・フッタ除去**: 配布元が付した前書き・後書き・商標表示を取り除き、
パブリックドメインの本文のみを用いる(除去後は配布元のライセンス適用対象外となる)。
`*** START ... ***`(27 行目)と `*** END ... ***`(19,007 行目)の内側 18,979 行のみを保持。
**残存参照 0 件を実測で確認した**(2026-09-04)。

出所記述は `raw/MANIFEST.json` の `provenance_statement_ja` を正本とし、画面の「底本について」に掲げる。

## 4. L0 実測(2026-09-04・設計より先に測った)

| # | 測ったこと | 実測値 |
|---|---|---|
| L0-1 | 章数 | S-A 21 = S-B 21 |
| L0-2 | 総語数(正規化後) | S-A **220,950** / S-B **208,201**。Freeman の記す約 213,000 語の帯に入る |
| L0-2b | **その差 12,749 語の内訳** | **本文 21 章だけで比べると S-A 206,599 / S-B 207,462 で差は −863 語(0.4%)**。差は全て章外で、S-A が目次・索引・出版社広告を含み(14,351 語)S-B が含まない(739 語)ことによる |
| L0-3 | 段落数 | S-A **3,170**(組版残滓 542 段落を除去後)/ S-B **1,140** |
| L0-4 | **段落の 1:1 対応は取れない** | S-A の `<p>` は頁で分断される(頁区切り 548 箇所)。**照合は語列で行う** |
| L0-5 | **二経路一致率(順序込み)** | 章別 21 件で中央値 **0.9542** / 最小 0.8759 / 最大 0.9931 |
| L0-6 | **二経路一致率(語彙・順序無視)** | 中央値 **0.9911** / 最小 0.9494 |
| L0-7 | L0-5 と L0-6 の差の原因 | **欠落ではなく並び**。S-B は翻刻注記どおり脚注を章末に集め、S-A は頁上の位置に置く。脚注の少ない第 14・18 章は順序込みでも 0.99 に達する |
| L0-8 | **S-A の翻刻異常** | 本文第 20 章の見出しが `CHAPTER XX.` ではなく **`CHAPTER XXX.`**(E-01) |
| L0-9 | S-A の組版残滓 | 頁番号・折丁記号(`2 F`・`2 G 2` 等)・`( 507 )` 形式を独立段落として持つ(**542 段落**)。除去の効果は **575 語**(221,525 → 220,950) |
| L0-10 | **1845 年版の索引は S-A にのみある** | 本体 **1,129 行**(見出し 747 / 継続 382)・異なり見出し語 **737**・頁参照 **1,635 件**(範囲展開後)・被覆 **447 頁**・相互参照 1 件 |
| L0-11 | **索引本体の終端は本が印刷している** | 末尾に `THE END.` があり、以降は印刷所の奥付と出版社広告。**辞書順や頁範囲から推定する必要はない** |
| L0-13 | **索引の並びは辞書順ではない** | 下位項目は非整列。さらに **J の項目群が I より前**に来る(1845 年の I/J 合併配列)。頭文字の逆転はこの 1 箇所のみ |
| L0-14 | 索引の頁参照の表記 | 個別頁のほか、範囲が **二表記**ある —— `N to M`(12 行)と `N—M`(2 行)。座標表記(`lat. 46° 40′`)の数は頁ではない(1 行) |
| L0-12 | **S-A の翻刻異常 その2** | 序文の署名が日の数字と地名行を欠く(`June , 1845.`)。S-B は完全(E-02) |

**L0-6 の帰結**: **G-14 の閾値は語彙保存(多重集合)に置く。順序込みの一致率に置いてはならない。**
順序込みで 0.95 を要求すると、正しい翻刻同士が落ちる(VERIF-FALSE)。

**L0-8 の帰結**: 翻刻異常は**黙って書き換えない**。章見出しは序数で対応づけ、
ローマ数字が序数と食い違ったら例外として報告する(AGENTS.md「仮定が崩れたら落ちる検算」)。

**L0-10 の帰結**: 1845 年に人が編んだ「地名・人名・種名 → 頁番号」の対応表が手に入る。
**LLM も本プロジェクトも一行も書いていない gold** として、抽出器の評価に使う(§7 O-1b)。

## 5. 未実測(**見込み**であり、確かめる手段を併記する)

以下は現時点で**測っていない**。断定として読んではならない。

| # | 見込み | 確かめる手段 |
|---|---|---|
| M-1 | §6 の目玉(陸にいた日が過半)が成立する | S-D を取得し、日単位で在地を数える(L2) |
| M-2 | S-C の Appendix から船の位置・日付表が機械可読に取れる | S-C を取得してパースする(L1) |
| M-3 | 歴史英語に対し MacBERTh が DistilBERT より低い fertility を示す | 両者の tokenizer を S-A 全文に当て、語あたりサブワード数と未知率を測る(L3) |
| M-4 | 段落埋め込みは int8 量子化で 2 MB 前後に収まる | 段落数確定後に実測する(L5) |

## 6. 事前登録した主張(目玉)

**主張: 『ビーグル号航海記』は航海の本ではない。**

日付が特定できる日のうち、ダーウィンが**陸にいた日が過半**であり、本文量も陸に偏る。

- 閾値 (a): 在地判定できた日のうち陸上日が **≥ 55%**
- 閾値 (b): 海上日 1 日あたり語数 ÷ 陸上日 1 日あたり語数 **≤ 0.7**
- 帰無モデル: 日ラベルを保ったまま章の割り当てを置換(seed 20260904・2,000 回)、**p ≤ 0.01**

**この主張は §7 の検算設計そのものである。** 船の航跡(S-C)と人の足跡(本文 + S-D)が
別系列であることを測る装置が、そのまま主張になる。地図上で二本の線が南米で大きく離れる絵は、
そのまま作品の主題である。

**閾値は実測を見る前に置いた値である**(S-D 未取得のため)。L2 で測った結果、
閾値の置き方が結論を決めていると判明した場合は、**主張を降ろして「測って捨てた」表に移す**。

### 第二の主張(落ちる見込みで登録する)

**ガラパゴスは記述密度の外れ値である。**

**分母の取り方で符号が入れ替わる見込みが強い** —— 絶対量では 21 章中 1 章(薄い)、
滞在日あたりでは濃い可能性がある。両方の分母で測り、**どちらに転んでも記録に残す**。

### 測って捨てた主張(記録として残す)

| # | 主張 | 実測 | 判定 |
|---|---|---|---|
| N-01 | 1839 年初版と 1845 年第 2 版の差分を測り、加筆がガラパゴスに集中することを示す | 1839 年版(F11)は Darwin Online で**画像・PDF のみで翻刻テキストが存在しない**(2026-09-04) | **不成立**。OCR が要り、差分の大半が OCR 誤りに溶ける。**第 2 版一本で通す** |

## 7. 検証の背骨 —— 循環の禁止

抽出を LLM で行い、評価も LLM で行えば循環する。本プロジェクトは
**本プロジェクトのために誰も書いていない正解**を外部に置く。

| ID | オラクル | 何を測るか | 循環しない理由 |
|---|---|---|---|
| **O-1a** | 章見出しの `--` 区切り要約行 | 抽出器の再現率 | 1845 年の著者・出版社が書いた。全 21 章を被覆する |
| **O-1b** | **1845 年版の索引**(L0-10) | 抽出器の再現率(語 → 頁) | 同上。異なり見出し語 737・頁参照 1,635 件・447 頁を被覆。成果物は `data/index_gold.json` |
| **O-2** | FitzRoy Appendix(S-C)の位置・日付表 | 復元した船の航路の位置精度 | 船長側の独立記録。本文を参照していない |
| **O-3** | 日次行程(S-D) | 日単位の在地判定 | 第三者(Rookmaaker)が編纂した別文書 |
| **O-4** | S-A × S-B の二経路照合 | 底本そのものの正しさ | 独立した二つの翻刻。**E-01 を実際に検出した** |

**三系列の分離**(提案時の「船が瞬間移動していないか」は成り立たない —— ダーウィンは
航海の相当部分を陸で過ごし、パンパスの騎行やアンデス越えは数週間に及ぶ。
帆走速度を人の系列に当てると**正しい内陸旅行を弾く**):

| 系列 | 出所 | 速度制約 | 地図での扱い |
|---|---|---|---|
| `ship` | S-C + S-D | 帆走 ≤ 10 kt | 主線(実線)。**抽出結果を使わない** |
| `darwin` | 本文の日付 + S-D | 騎行 ≤ 60 km/日、徒歩 ≤ 30 km/日 | 破線 |
| `mention` | 本文抽出のみ | 制約なし | **航路に載せない**(回想・比較・伝聞) |

## 8. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 底本の二経路照合の結果(章別一致率・既知の翻刻異常)を「底本について」に出す | must |
| F-02 | 世界地図に `ship` と `darwin` の二系列を重ねて描く。経度は unwrap し太平洋横断を一本の線にする | must |
| F-03 | 年表(1831–1836)。レーンは `航海 / 陸上行 / 地質 / 生物 / 社会`。ブラシ選択が地図に反映される | must |
| F-04 | 地図・年表のクリックで該当箇所を原文表示し、抽出根拠の文を強調する | must |
| F-05 | 目玉(§6)の検定結果を帰無分布とともに一枚で出す | must |
| F-06 | 滞在日数 × 記述量の散布図(第二の主張の検証図) | should |
| F-07 | 和訳は**段落単位の段階的充填**。**充填率を分子/分母で画面に出す**。未訳段落は原文のみ表示し、隠さない | must |
| F-08 | 日本語クエリによるクロスリンガル段落検索(ビルド時に埋め込みを焼き、ブラウザは内積のみ) | should |
| F-09 | 構造化データ(航路 / イベント / 地名辞書)を JSON と CSV で配る | must |
| F-10 | フリート共通フッタ(5 項目・下部固定) | must |
| F-11 | 全画面が静的書き出しで動く(関数実行・ランタイム外部呼び出しゼロ) | must |

## 9. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 本文・ソースにキリル文字・制御文字が混入しない | `python harness/text_hygiene.py` |
| N-02 | **和訳は共有の字種検査の射程外なので、自前の検査を持つ** | 訳文専用の字種・記号検査(G-12) |
| N-03 | 320px / 390px / 768px / 1280px で横溢れが無い | `npm run smoke`(実ブラウザ) |
| N-04 | 出荷ビルドが通る(型検査 + static export) | `npm run verify` |
| N-05 | ETL は決定的(同一入力・同一 seed で bit 一致) | 二回実行して比較(G-08) |

## 10. 品質ゲート

| ID | 内容 | 閾値 |
|---|---|---|
| **G-01** | 版の確定 | S-A・S-B とも序文日付が 1845-06-09。章数 21 = 21 |
| **G-02** | 配布元ヘッダ・フッタの除去 | S-B 本文中の商標残存参照 **0 件** |
| **G-14** | **底本の二経路照合** | 全 21 章で**語彙保存(多重集合)≥ 0.94**。順序込みの一致率は**参考値として記録するのみで閾値にしない** |
| G-14b | 既知の翻刻異常の検出 | E-01(`CHAPTER XXX`)を検査が実際に報告する(陽性対照) |
| G-02b | 章見出し要約行の全章抽出 | 21 章すべてで `--` 区切りが解け、要素 0 個の章が無い |
| G-03 | **O-1b 再現率** | 索引の見出し語のうち本文から拾えた率 ≥ 0.90(章別に出す) |
| G-04 | **O-2 位置照合** | FitzRoy Appendix と一致する日で中央値誤差 ≤ 50 km |
| G-05 | **O-3 在地照合** | 日次行程と日単位で不一致 ≤ 5% |
| G-06 | 三系列の速度制約 | `ship` ≤ 10 kt / `darwin` ≤ 60 km/日 / `mention` は航路に載らない |
| G-07 | 生徒 vs 教師 | 生徒 F1 ≥ 教師 LLM の 95%、かつ**辞書マッチ + 正規表現のベースラインを上回る** |
| G-08 | 決定性 | 同一入力・同一 seed で ETL 出力が bit 一致(2 回実行) |
| **G-09** | **目玉** | §6 の (a)(b) と置換検定 `p ≤ 0.01` の三つとも満たす |
| G-10 | 検出系テストに陽性対照が対で存在する | テスト一覧の走査 |
| G-11 | 図のラベルが図と同じデータから導かれる | 座標決め打ちの検出テスト(陽性対照つき) |
| G-12 | 訳文の字種・記号検査(N-02) | 違反 0 件 + 陽性対照 |
| G-13 | 出荷ビルド | 型検査 + static export が通る。関数 0・cron 0 |

## 11. パイプライン

```
raw/          S-A / S-B / S-C / S-D(manifest に URL・取得日・sha256・加工手順を固定)
  ↓ 01_segment    章・段落分割 + 章見出し要約行の抽出 + 索引のパース → O-1a / O-1b が同時にできる
  ↓ 02_track      S-C + S-D から船の航路          ← 抽出を使わない。ここまでで地図が描ける
  ↓ 03_extract    LLM 抽出(章単位・スライディング窓) → 弱教師ラベル
  ↓ 03b_distill   [PyTorch] 蒸留学習 → 生徒モデル
  ↓ 04_apply      [PyTorch] 生徒で全章再抽出(決定的・API 費 0)
  ↓ 05_resolve    地名 → 座標(編集距離 + 手動マッピング表)
  ↓ 06_validate   O-1a/O-1b 再現率・O-2 位置・O-3 在地・三系列の速度制約
  ↓ 07_embed      [PyTorch] 段落埋め込み → int8 量子化
  ↓ 08_build
app/          Next.js / static export・関数 0・cron 0
```

**`02_track` を `03_extract` より前に置く。** 地図と年表は外部の権威表だけで完成し、
抽出はその上に本文への逆引きを載せる。**抽出精度が公開をブロックしない。**

## 12. 段階計画

| L | 内容 | 完了条件 |
|---|---|---|
| **L0** | 底本の二経路取得と照合、SPEC・ゲート凍結 | G-01 / G-02 / G-14 / G-14b 通過 |
| L1 | S-C・S-D 取得、`01_segment` + `02_track`、地図・年表の骨 → **デプロイ** | G-02b / G-04 通過・本番が見える |
| L2 | 在地判定と目玉の検定 | G-05 / G-09 判定(落ちたら「測って捨てた」に移す) |
| L3 | ブラジル篇で LLM 抽出、O-1a/O-1b で採点、基盤モデルの fertility 実測(M-3) | G-03 が 1 章で通る |
| L4 | 全章 LLM 抽出 → 弱教師ラベル | 21 章分の JSONL |
| L5 | 蒸留(学習・評価・誤り分析・アブレーション) | G-07 通過・誤り分析が文書化 |
| L6 | 生徒で全章再抽出、`06_validate` 全通し | G-06 / G-08 通過 |
| L7 | 日本語クロスリンガル検索、和訳充填、app-menu 掲載 | G-12 / G-13 通過 |
| L8+ | 章単位で和訳を充填(1 ループ 1〜2 章) | 毎ループ G-12 を全訳済み範囲に通す |

**歯止めは L1 に置く。** §11 の並べ替えにより、抽出も蒸留も一行も書かないうちに
公開可能な作品(航路 + 年表 + 章立て)が成立する。以後の全段階は
「載る情報が増える」だけで、**未完成で止まっても公開物が壊れない**。

## 13. スコープ外

- **1839 年初版との差分**(§6 N-01。翻刻テキストが存在しない)
- **全訳を公開の前提にすること**(段階的充填とし、充填率を出す)
- **既存邦訳の参照・照合**(著作権が生きている。訳の検査に使わない)
- **訳文を抽出器の入力にすること**(循環する。抽出は常に英語原文に対して行い、訳文は表示専用)
- 航路の補間・予測モデル(実測の寄港地表がある)
- 歴史的地名の正規化にニューラルモデル(編集距離 + 手動表で片付く)
- ランタイムでの推論(関数 0 の方針)
- 3D globe(年表連動 UI を主役にするため 2D で通す)
