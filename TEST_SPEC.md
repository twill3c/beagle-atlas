# TEST_SPEC.md — beagle-atlas

<!-- scaffold template v1.26.0 から展開(2026-09-04) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(直交性・一意性・単一帰属等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

合成フィクスチャだけでなく、**外部データ・外部権威に対する期待値**にも同じ規律が要る。
ケースごとに、その期待値がどこから来たのかを書くこと。

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない**(超えるなら先に SPEC を上げる) |
| 外部権威(公表値・規格・検証データ) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す。「誰かの仮定」と区別できるようにする |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。
外部データは重複や欠落を含むため、数はデータが動くたびに壊れる。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `raw/f14.converted.html`(S-A) | Darwin Online, F14 の翻刻。取得 2026-09-03・sha256 は `raw/MANIFEST.json` | 一次資料の翻刻(正本) |
| `raw/pg944.body.txt`(S-B) | Project Gutenberg #944 のプレーンテキストから配布元ヘッダ・フッタを除去したもの。取得 2026-09-03 | **独立した第二の翻刻**。S-A の検算に使う |
| 章見出しの `--` 区切り要約行 | S-A / S-B の本文そのもの(1845 年の著者・出版社が書いた) | **O-1a** 抽出器の再現率オラクル。本プロジェクトのために誰も書いていない |
| 1845 年版の索引 | S-A のみが収録(L0-10) | **O-1b** 抽出器の gold。1845 年に人が編んだ「語 → 頁」の対応表 |
| Freeman の書誌記述(約 213,000 語) | Darwin Online, Freeman "Journal of Researches" 解題。取得 2026-09-03 | 総語数の帯を与える外部権威 |

**S-A と S-B は独立に翻刻された同一版である。** どちらか一方を「正しい」と仮定して
他方を採点するのではなく、**両者の食い違いを検出して台帳に出す**のが本ゲートの役割である。
実際に E-01(S-A 第 20 章の見出しが `CHAPTER XXX`)をこの方法で検出した(2026-09-04)。

## 既知の翻刻異常(台帳)

| ID | 側 | 箇所 | 観測 | あるべき値 | 扱い |
|---|---|---|---|---|---|
| E-01 | S-A | 本文 第 20 章 見出し | `CHAPTER XXX.` | `CHAPTER XX.` | **黙って書き換えない**。序数で対応づけ、異常として報告する |
| E-02 | S-A | 序文の署名 | `June , 1845.` | `DOWN, BROMLEY, KENT, June 9, 1845` | 日の数字と地名行を欠く。**版の確定は年で行い、完全な日付は S-B から採る**。翻刻が直る可能性があるので閾値化しない |

**E-02 は T-001 の失敗として現れた**(2026-09-04)。期待値「序文に `June 9, 1845` がある」を
**S-B だけを観測して両経路に書いた**のが原因である(VERIF-FALSE / HC-016
「観測の義務は自分で書いた例文にも及ぶ」の外部データ版)。実装は正しく、テスト側が誤っていた。

## ケース一覧

| ID | 対応要求 | ケース | 期待 | 期待値の出所 |
|---|---|---|---|---|
| T-001 | G-01 | 序文の版確定根拠が読める | **両経路で序文が 1845 年付**、かつ **S-B に完全な日付**がある | SPEC §3(本文実測 2026-09-04)。S-A は E-02 により日を欠くので、両経路に要求するのは年まで |
| T-002 | G-01 | 本文側の章見出しが両経路とも 21 件 | 21 = 21 | 実測 2026-09-04(L0-1) |
| T-003 | G-02 | S-B 本文に配布元の商標残存参照が無い | 0 件 | SPEC §3・実測 2026-09-04 |
| T-004 | G-02 | **陽性対照**: 除去前の生ファイルには残存参照がある | 1 件以上 | 検査器が働いていることの確認(G-10) |
| T-005 | G-14 | 全 21 章で語彙保存(多重集合)が閾値以上 | 全章 ≥ 0.94 | SPEC §10 G-14。閾値は L0-6 の実測(中央値 0.9911・最小 0.9494)に余裕を取った値 |
| T-006 | G-14 | 順序込み一致率は**記録するのみで閾値にしない** | 21 章分の値が得られる | L0-7(差は脚注の並びに由来し、欠落ではない) |
| T-007 | G-14b | **陽性対照**: E-01 を検査が実際に報告する | 第 20 章が異常として報告される | 台帳 E-01(実測 2026-09-04) |
| T-008 | G-14b | 台帳に無い章では異常が報告されない | 第 20 章**以外**の異常は 0 件 | 検査器が撃つべきでないものに撃たないこと(G-10) |
| T-009 | L0-9 | S-A の組版残滓(頁番号・折丁記号)が除去される | 除去後に純粋な数字のみの段落が残らない | 実測 2026-09-04(541 段落) |
| T-010 | L0-2 | 総語数が Freeman の記す帯に収まる | 両経路とも 190,000–240,000 語 | Freeman 解題の約 213,000 語に対し ±13% の帯。**SPEC の保証粒度(帯)を超えない** |
| T-011 | L0-10 | S-A に索引があり、S-B には無い | S-A のみ検出 | 実測 2026-09-04 |
| T-012 | N-01 | 本文・ソースにキリル文字・制御文字が混入しない | 違反 0 件 | `harness/text_hygiene.py` |

## 索引のパース(O-1b / G-03 の前提)

**測ってから書いた**(HC-069「分割器はテストより先に実データ全域で被覆を測る」)。
2026-09-04 の全域走査。**数はすべて索引本体(`INDEX.` の次行から `THE END.` の手前まで)を
母集団とする**: 本体 1,129 行(見出し 747・継続 382)、頁参照 1,438 件(範囲 1–499)、
`see` 参照 1 件、`N to M` 形式 **12 件**、異なり見出し語 737、範囲展開後の頁参照 1,628 件。

> **母集団に注意**(HC-152)。`THE END.` 以降には出版社広告が続き、そこにも `1832 to 1836` の
> ような表記がある。広告まで含めて数えると `N to M` は 17 件になる。**索引本体の数は 12 件**。
> 最初この 17 を本体の数として書き、パーサの出力(12)と食い違ったときに**先にパーサを疑った**。

**この索引について実データが教えた二つの事実**(推測で書くと必ず外す):

- **辞書順は成り立たない。** 下位項目は整列されておらず(`Azara on spiders` の次が
  `Azara on range of carrion-hawks`)、さらに **J の項目群が I より前に来る**
  (1845 年の組版に見られる I/J 合併配列)。素朴な辞書順検査は正しい索引を落とす
- **終端は印刷されている。** 索引の末尾に `THE END.` があり、その後は印刷所の奥付と
  出版社広告が続く。**辞書順や頁範囲から終端を推定する必要はない**

| ID | 対応要求 | ケース | 期待 | 期待値の出所 |
|---|---|---|---|---|
| T-020 | O-1b | 索引本体の終端が印刷標識で決まる | 本体は `THE END.` を含まず、その手前で終わる | 実測 2026-09-04(本体 1,129 行) |
| T-021 | O-1b | **全行がいずれかの型に分類される** | 未分類 0 件。分類できない行は黙って捨てず例外で止まる | AGENTS.md「分類できなかったものは例外にして止める」 |
| T-022 | O-1b | 継続行は必ず親の見出し語を継承する | 索引の先頭行は継続行でない。全継続行に親がある | 実測 2026-09-04(継続 382 行) |
| T-023 | O-1b | 全ての頁参照が本の頁範囲に収まる | 範囲外 0 件。**範囲は `[page N]` マーカーから導出し定数で書かない** | S-A の頁マーカー(実測 1–536) |
| T-024 | O-1b | **陽性対照**: 頁範囲検査が範囲外の値を撃つ | 合成した範囲外の項目が検出される | 検査器が働いていることの確認(G-10) |
| T-025 | O-1b | `N to M` 形式が範囲として解釈される | 範囲を持つ項目が存在し、すべて start < end。取りこぼし 0 | 実測 2026-09-04(**索引本体で 12 件**) |
| T-026 | O-1b | 相互参照(`see`)は頁を持たない項目になる | 頁 0 件・参照先を保持 | 実測 2026-09-04(1 件) |
| T-027 | O-1b | 頭文字は **I/J の入れ替えを除いて**単調 | 既知の入れ替え以外に逆転が無い | 実測 2026-09-04(逆転は I/J の 1 箇所のみ) |
| T-028 | O-1b | **陽性対照**: 単調性検査が壊した並びを撃つ | 入れ替えた合成列で逆転が報告される | 検査器が働いていることの確認(G-10) |
| T-029 | O-1b | gold として使える | 頁を持つ項目が本体行数の 99% 以上 | 実測 2026-09-04(数字を含まない行は 1 件のみ) |
